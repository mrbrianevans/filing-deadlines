{$SITE_ADDRESS} {

    log {
        output stdout
        format console
    }

	handle /api/* {
		reverse_proxy server:{$SERVER_PORT}
	}


	handle {
        root * /client
	    encode gzip
        try_files {path} /index.html
        file_server
    }

}
